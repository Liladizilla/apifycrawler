{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Location-based business lookup actor input",
  "type": "object",
  "properties": {
    "destination": {
      "type": "string",
      "description": "Area, city, district, or neighborhood to search in. Example: Nairobi, Westlands, Kisumu."
    },
    "search_term": {
      "type": "string",
      "description": "What the user is looking for in that area. Example: hotel, salon, restaurant, gym."
    },
    "location": {
      "type": "string",
      "description": "Optional alias for destination."
    },
    "query": {
      "type": "string",
      "description": "Optional alias for search_term."
    },
    "max_results": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "description": "Number of businesses to return."
    },
    "startUrls": {
      "type": "array",
      "description": "Legacy direct URL batch mode for known businesses.",
      "items": {
        "type": "object",
        "properties": {
          "business_name": { "type": "string" },
          "website": { "type": "string" },
          "url": { "type": "string" },
          "name": { "type": "string" }
        },
        "required": ["url"]
      }
    },
    "csv_file": {
      "type": "string",
      "description": "Optional CSV path for a direct business batch. Kept only for compatibility; the repo does not track sensitive company lists."
    },
    "google_places_enabled": {
      "type": "boolean",
      "description": "Set to true only when you have authorized Google Places API access and a valid API key configured."
    },
    "google_places_api_key": {
      "type": "string",
      "description": "Optional Google Places API key used only when the actor is explicitly enabled for authorized retrieval."
    }
  }
}
